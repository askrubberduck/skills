interface:
  display_name: "Duck Campaign"
  short_description: "Turn the grand vision into work that can ship"
  default_prompt: "Use $askrubberduck:duck-campaign to turn this vision into work that can ship. Split where it helps, give each unit a finish line, and keep ready work moving."
