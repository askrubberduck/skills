interface:
  display_name: "Duck Campaign"
  short_description: "Turn the grand vision into work that can ship"
  default_prompt: "Use $askrubberduck:duck-campaign to carve this vision into independent workstreams. Give each a finish line; keep ready work moving."
