interface:
  display_name: "Duck Dry"
  short_description: "Keep the facts the code cannot speak for itself"
  default_prompt: "Use $askrubberduck:duck-dry to strip this prose to the facts the code cannot carry. Keep the traps and contracts; check that nothing broke."
