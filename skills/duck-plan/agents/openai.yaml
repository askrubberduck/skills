interface:
  display_name: "Duck Plan"
  short_description: "Find the hole in the plan before building over it"
  default_prompt: "Use $askrubberduck:duck-plan to shape this plan and try to falsify its assumptions before implementation. Name the checks that could stop it."
