interface:
  display_name: "Duck Plan"
  short_description: "Plan necessary changes and checks that can expose mistakes"
  default_prompt: "Use $askrubberduck:duck-plan to identify the necessary changes, cut speculative tasks, and name the checks that could expose a wrong implementation."
