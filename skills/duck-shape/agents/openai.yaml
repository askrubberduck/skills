interface:
  display_name: "Duck Shape"
  short_description: "Remove unnecessary code while preserving required behavior"
  default_prompt: "Use $askrubberduck:duck-shape to remove unnecessary mechanisms and duplicate rules from this code, preserve its required behavior, and verify the result."
