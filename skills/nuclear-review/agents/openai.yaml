interface:
  display_name: "Nuclear Review"
  short_description: "Get an independent cross-model change review"
  default_prompt: "Use $askrubberduck:nuclear-review to get an independent cross-model review before this change ships."
